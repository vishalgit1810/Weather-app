const testServerController = require('./test-server-controller');
const defaultCont = require('./weather-controllers');
const getWeather = require('./get-weather-controller');

module.exports = {
    testServerController,
    defaultCont,
    getWeather
};


